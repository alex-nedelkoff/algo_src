# Dataset files (large, downloaded separately)
state_estimation/data/

# VIO evaluation results
state_estimation/results/

# Python
__pycache__/
*.egg-info/
.venv/

# Credentials & local overrides
.env
docker-compose.override.yml

# Experiment outputs
wandb/
outputs/

# Large exported assets (FBX, point clouds, etc. — reconstruct on demand)
data/

# Built warehouse URDF assets (reconstructible from data/ + configs/, see sim/assets/README.md)
sim/assets/warehouse_fab_v1/

# 3DGS scenes (large, downloaded from HF) and ETH3D real-camera benchmark
# (downloaded from eth3d.net). See debug_mast3r_3dgs_*.py / debug_mast3r_eth3d_slam.py
sim/assets/3dgs/
sim/assets/eth3d/

# PyBullet GUI runtime tracing dumps
timings_*.json

# Git worktrees
.claude/worktrees/
carwelding_depth_left.zip

# External packages cloned at bootstrap (MASt3R-SLAM checkpoint is 2.75 GB,
# code is ~500 MB). See scripts/cloud/bootstrap.sh for clone instructions.
external_packages/
