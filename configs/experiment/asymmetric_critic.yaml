# @package _global_
# Asymmetric actor-critic: privileged critic with full sim state

defaults:
  - override /sim: numpy_quad
  - override /control: ppo_asymmetric
  - override /perception: none
  - override /reward: monorace
  - override /domain_rand: uniform_30pct
  - override /logging: wandb

seed: 42
total_timesteps: 10_000_000

logging:
  tags: [asymmetric, privileged_critic]
  group: asymmetric_critic
