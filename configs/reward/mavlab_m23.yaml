# MAVLab M23 reward preset (safe: prioritizes progress + crash avoidance)
preset: M23

# These are informational — the preset string is used directly by compute_reward
weights:
  lambda_gate: 1.5
  lambda_offset: 1.5
  lambda_perc: 0.0
  lambda_prog: 1.0
  lambda_rate: 0.001
  lambda_delta_u: 0.001
  lambda_crash: 10.0
  lambda_alive: 0.0
  v_max: 10.0
